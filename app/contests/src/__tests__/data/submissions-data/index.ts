import submissionInput from './submissions-input.json';
import incorrectSubmissionSolution from './incorrect-submission-input.json';
import invalidFunctionSubmissionInput from './invalid-function-submission-input.json';

export {
    submissionInput,
    incorrectSubmissionSolution,
    invalidFunctionSubmissionInput
}